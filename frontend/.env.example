# Copy to frontend/.env for local dev, or set in your host's build settings.

# Where the frontend should send API requests. Leave unset for local dev
# (defaults to http://localhost:8000). In production, point at your deployed
# backend, e.g. https://api.vetconnect.example.com
VITE_API_BASE_URL=
